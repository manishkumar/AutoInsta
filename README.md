# Insta-automator

# Dependencies
Run `npm install` to install dependencies.

# Configuration
Create `config.json` from the given `config.json.dist` file and enter account credentials. Other configuration items can also be changed from this file.

The script reads instagram handles from `handles.json` array and follows them after login with given credentials.

# Running the script
`node build/index.js`